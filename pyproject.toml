[project]
name = "kiro-orchestration"
version = "0.1.0"
description = "Event-driven orchestration for Kiro CLI sessions"
requires-python = ">=3.10"

dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
